# AGENTS.md

本文件适用于整个 Questra 仓库。目标是在尽量少猜测的前提下，交付与现有架构一致、经过验证且文档准确的改动。

## 开始前

1. 先阅读 `git status`、相关源码、测试、配置、`package.json` 脚本及相关文档；不要覆盖用户已有改动。
2. 写代码前判断相关模块是否存在会阻碍本次开发的结构、重复或质量问题。必要时做小范围前置优化并说明原因；非必要重构只记录建议，不扩大任务范围。
3. 优先沿用现有技术和模式：CommonJS 后端、Express、SQLite、React、Vite，以及仓库已有工具函数和目录职责。
4. 只有当关键需求无法从代码或文档确认，且不同选择会明显改变行为、兼容性或工作量时，才询问用户。提问时给出上下文、推荐选择及主要取舍。

## 实现规则

- 改动应紧贴需求，保持现有 API、CLI、配置和数据兼容性；需要破坏兼容时必须提前说明。
- API 变更应同时覆盖路由、鉴权、参数、响应、错误状态和测试，并提供请求/响应示例。
- 数据库结构变化必须新增顺序迁移文件；不要修改已经发布的迁移。
- CLI、环境变量或配置变化应同步更新配置示例和使用说明。
- 新增依赖、工具、框架或架构前，先确认现有方案不能合理解决问题。采用后必须说明：
  - 它解决什么问题；
  - 为什么需要它；
  - 如何安装、配置和运行；
  - 对构建、部署、兼容性和维护的影响。
- 考虑 Windows、Linux、macOS 的路径、Shell 和进程管理差异；不要把单一平台命令写成通用命令。

## 文档同步

1. 代码完成后搜索全部 Markdown 文档，更新每一处受影响的行为、命令、路径、配置和示例，避免文档与实现不一致。
2. `README.md` 保持简洁，面向首次安装和使用；深入部署、API、插件和开发内容放在 `docs/guide.md` 或对应专题文档。
3. 新增或修改 API、CLI、配置、环境变量、迁移、插件钩子、构建或部署方式时，相关文档是交付的一部分，不应留到以后补写。
4. 文档中的命令和示例必须与当前代码相符；能执行验证的命令应实际验证。

## 验证

- 先运行与改动直接相关的测试，再根据影响范围运行完整检查。
- 常规完整检查使用 `npm run check`。
- 前端生产行为变化还应运行 `npm run build`。
- npm 发布内容、README 或 `package.json#files` 变化还应运行 `npm pack --dry-run` 或 `npm run release:check`。
- 结束前运行 `git diff --check` 并复查 `git status`，确认没有遗漏源码、迁移、测试或文档。
- 无法执行某项验证时，明确说明原因和剩余风险；不要把未运行写成已通过。

## 交付说明

最终回复应简要说明：完成了什么、关键设计决定、文档更新、验证结果，以及新增工具或架构的用途。若仍需用户选择或执行交互式操作，给出清晰的下一步。
