# AGENTS.md

本文件适用于整个 Questra 仓库，约定开发者与 AI 如何共同完成可靠、克制且可验证的改动。

## 1. 基本原则

- 先理解现状，再决定改动。结论应来自代码、配置、测试和文档，而不是从文件名或局部实现猜测。
- 尊重用户已有工作，根据用户需求考虑是否修改未提交改动。不覆盖无关内容，不使用破坏性操作清理工作区。
- 选择能完整解决问题的最小改动范围。不要借当前任务进行无关重构、批量改名或格式化。
- 把兼容性视为设计约束。已有 API、CLI、配置、数据和安装方式应继续可用；确需破坏兼容时，先说明影响和迁移路径。
- 只有当不同选择会显著改变行为、兼容性或工作量，并且无法从仓库确定答案时，才请求用户决策。

## 2. 理解影响范围

开始修改前，先确认当前分支、未提交改动、相关入口和现有约定。围绕需求追踪定义、调用方、用户界面、测试、配置、迁移、构建与发布边界，形成一份足以解释“哪里会受影响、哪里不会”的内容地图。

不同类型的改动至少关注以下关系：

- API：路由、鉴权、输入校验、响应结构、错误状态、调用方和契约示例。
- CLI 与配置：命令行为、参数优先级、运行状态、示例配置、跨平台差异和用户文档。
- 数据库：新旧数据库升级、事务、备份恢复和历史数据兼容。已发布迁移不可回写，只能新增有序迁移。
- 前端：真实后端契约、加载与错误状态、响应式布局、生产构建和无构建产物时的兼容界面。
- 构建与发布：源码开发、发布包白名单、锁文件、生成产物以及最终用户安装方式。

发现重复、耦合或缺陷时，只有它确实阻碍当前需求，才在同一改动中做小范围修正；其余问题留在交付说明中。

## 3. 方案驱动的开发流程

收到需求并完成初步代码阅读后，先评估改动的复杂度、风险和影响边界，在 `docs/proposal/` 中建立或更新对应的 Markdown 方案书，再开始实现。方案书是本次工作的执行依据，不是脱离代码现状的设想；同一需求继续调整时应更新原方案，只有目标或边界已经独立时才新建文件。

方案书的详细程度应与任务相称：

- 简单改动使用精简方案，写清目标、受影响位置、实现步骤和验收条件即可。
- 涉及多个模块、数据结构、公共契约、兼容性、安全或发布流程的改动使用完整方案，说明现状、范围、关键决定、分阶段步骤、迁移与回退、验证和文档归类。
- 纯文字勘误、内容重排等不影响运行行为且无需分步实施的修改，可以使用简短方案记录，不为满足形式重复描述显而易见的操作。

编写和执行方案时遵循以下流程：

1. 依据当前代码和配置记录问题、目标、非目标、受影响边界及兼容性要求。
2. 把实现拆成可独立检查的有序步骤，并为每一步写明预计修改位置、依赖关系和完成条件。
3. 在涉及不同设计取舍时记录决定及理由；无法从仓库确定且会显著改变结果的事项，先标为待决并请求用户确认。
4. 按方案顺序实现。发现原判断不成立、范围改变或出现新的必要步骤时，先修订方案，再继续修改代码。
5. 实施期间及时维护步骤状态和偏差说明，不把尚未完成或尚未验证的内容标记为完成。
6. 完成后按实际影响执行必要验证，回填结果，并将面向用户、管理员、开发者或运维者的稳定信息归入对应专题文档。方案书保留设计背景、执行记录和未进入正式文档的决策依据。

`docs/proposal/README.md` 负责方案索引与状态说明。文件名应简洁表达需求主题；方案中不得写入密钥、真实 Token、个人数据或只能在当前机器成立的临时信息。

## 4. 实现约束

- 沿用仓库已有模块边界和代码风格。新增抽象必须减少真实复杂度或消除有风险的重复。
- 新增依赖前，确认现有能力不能合理解决问题，并评估安装、构建、部署、兼容性和维护成本。
- 开发与 CI 以 Node.js 22 为基线；源码开发推荐 pnpm，但 npm 仍需可用。`pnpm-lock.yaml` 是仓库依赖基线，其他包管理器生成的锁文件不提交。
- 通用脚本不得依赖某一种包管理器的命令；依赖变化必须更新仓库锁文件，业务代码或文档改动不应产生锁文件噪声。
- 路径、环境变量、权限和进程管理相关设计必须考虑 Windows、Linux 与 macOS，避免只在当前终端成立的方案。
- 本地配置、数据库、Token、日志、依赖目录、构建缓存和临时文件不得进入版本库或发布包。

## 5. 文档信息架构

文档不是代码改动后的附属清单。行为变化时，先判断信息面向谁、解决什么任务、哪份文档是该信息的主要来源，再决定放置位置并消除冲突或重复。

- `README.md`：面向首次接触 Questra 的用户，只保留产品介绍、三种安装方式、快速开始、最常用命令和专题文档入口。
- `docs/installation.md`：面向安装者和运维者，解释安装、配置、进程托管、HTTPS、升级、迁移、备份恢复和故障排查。
- `docs/usage.md`：面向站点管理员，按照欢迎向导、题库、问卷/考试、发布、答卷和设置的实际工作流说明产品。
- `docs/development.md`：面向贡献者和扩展开发者，说明架构、源码工作流、模块职责、数据迁移、扩展钩子、测试和发布机制。
- `docs/api.md`：作为 HTTP 契约参考，集中维护鉴权、端点、请求/响应、错误和安全边界。
- `docs/README.md`：只负责文档导航和阅读路径，不复制专题正文。
- `docs/proposal/`：保存尚待实施或正在实施的需求方案、阶段状态和设计决定；它不是稳定功能说明的替代品。

新增内容应进入最贴近读者任务的主要文档，其他位置只保留简短上下文和链接。不要用“同步更新文档”只对文档增量更新，却免去了内容归类；也不要让相同规则在多处形成互相漂移的完整副本。示例必须与当前行为一致，依赖外部条件的步骤应明确前置条件。

此外，如果分类过于牵强或的确无法分类，务必单开一个新文件。

## 6. 验证与完成

验证只针对会改变 Questra 运行行为或交付结果的代码与文件。仅修改 Markdown、说明文字、注释或其他纯文本内容时，不运行测试、lint、构建、打包或链接检查；完成内容归类和改写即可。

需要验证时，应先完成本轮相关改动，再按实际影响范围集中执行一次必要检查，避免在每个小步骤后重复运行同一流程：

- 局部业务逻辑只运行覆盖该模块或行为的测试，必要时检查相关 lint。
- API、CLI、配置或共享模块改动应覆盖直接契约及受影响调用方，不自动扩大到无关模块。
- 前端行为改动只运行相关前端测试；只有影响生产产物时才执行前端构建。
- 数据库迁移应覆盖空库初始化和已有库升级，不需要因此重复运行无关前端检查。
- 依赖、构建脚本、发布配置或包内容发生变化时，才检查对应的安装、构建或发布边界。
- 只有跨越多个共享边界、准备正式发布、难以可靠界定影响范围，或用户明确要求时，才运行完整检查流程。

完成前重新审视：

- 需求涉及的旧名称、旧路径、旧命令和旧链接是否仍有遗漏。
- 新文件是否被正确追踪、忽略或纳入发布包。
- 最终差异是否只包含预期内容，且没有秘密、运行数据或生成噪声。
- 已执行的必要检查是否通过；应执行但无法运行的检查必须如实说明及其剩余风险。

交付说明应简要列出完成内容、关键设计与兼容性决定、文档归类，以及仍需用户执行的外部或交互步骤。只有实际执行过验证或存在未覆盖风险时，才说明验证结果。
